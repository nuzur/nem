// Code generated by nuzur go-code-gen. DO NOT EDIT.

package team

import (
	"context"
	"database/sql"
	"fmt"
	main_entity "github.com/nuzur/nem/core/entity/team"
	"github.com/nuzur/nem/core/module/team/types"
	repogen "github.com/nuzur/nem/core/repository/gen"

	"go.uber.org/zap"
	"slices"
)

func (m *module) List(ctx context.Context,
	request types.ListRequest,
	opts ...Option) (types.ListResponse, error) {

	reqPlusOne := request
	reqPlusOne.PageSize = request.PageSize + 1

	query, err := m.repository.BuildListEntityQuery(
		ctx,
		reqPlusOne,
		main_entity.Team{},
		false)
	if err != nil {

		return types.ListResponse{}, err
	}

	resolvedOpts := applyAllOptions(opts)
	cacheKey := fmt.Sprintf("ListTeam:%v", request)
	if !resolvedOpts.SkipCache {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.ListResponse), nil
		}
	}

	v, listErr, _ := m.sg.Do(cacheKey, func() (any, error) {
		var rows *sql.Rows
		rows, err = m.repository.DB.QueryContext(ctx, query)
		if err != nil {

			m.logger.Error("error in executing query for ListTeam", zap.String("query", query), zap.Error(err))
			return types.ListResponse{}, err
		}
		defer rows.Close()
		var scanGetters []func(*repogen.Team) any
		if len(request.GetIncludeFields()) > 0 {
			for _, f := range listFields {
				if slices.Contains(request.GetIncludeFields(), f) {
					scanGetters = append(scanGetters, listFieldRegistry[f])
				}
			}
		} else if len(request.GetExcludeFields()) > 0 {
			for _, f := range listFields {
				if !slices.Contains(request.GetExcludeFields(), f) {
					scanGetters = append(scanGetters, listFieldRegistry[f])
				}
			}
		} else {
			for _, f := range listFields {
				scanGetters = append(scanGetters, listFieldRegistry[f])
			}
		}

		var items []repogen.Team
		for rows.Next() {
			var i repogen.Team
			fields := make([]any, 0, len(scanGetters))
			for _, getter := range scanGetters {
				fields = append(fields, getter(&i))
			}
			if err := rows.Scan(fields...); err != nil {

				return types.ListResponse{}, err
			}
			items = append(items, i)
		}
		if err := rows.Close(); err != nil {

			return types.ListResponse{}, err
		}
		if err := rows.Err(); err != nil {

			return types.ListResponse{}, err
		}

		hasNextPage := false
		if len(items) > int(request.PageSize) {
			hasNextPage = true
			items = items[:request.PageSize]
		}

		return types.ListResponse{
			Team:        mapModelsToEntities(items),
			HasNextPage: hasNextPage,
		}, nil
	})
	if listErr != nil {
		return types.ListResponse{}, listErr
	}
	listResponse := v.(types.ListResponse)
	if !resolvedOpts.SkipCache {
		m.cache.Set(cacheKey, listResponse, 0)
	}
	return listResponse, nil
}

var listFields = []string{

	"uuid",

	"version",

	"name",

	"enviorments",

	"review_configs",

	"stores",

	"connections",

	"object_stores",

	"default_entity",

	"owner_uuid",

	"status",

	"created_at",

	"updated_at",

	"created_by_uuid",

	"updated_by_uuid",
}

var listFieldRegistry = map[string]func(*repogen.Team) any{

	"uuid": func(i *repogen.Team) any { return &i.UUID },

	"version": func(i *repogen.Team) any { return &i.Version },

	"name": func(i *repogen.Team) any { return &i.Name },

	"enviorments": func(i *repogen.Team) any { return &i.Enviorments },

	"review_configs": func(i *repogen.Team) any { return &i.ReviewConfigs },

	"stores": func(i *repogen.Team) any { return &i.Stores },

	"connections": func(i *repogen.Team) any { return &i.Connections },

	"object_stores": func(i *repogen.Team) any { return &i.ObjectStores },

	"default_entity": func(i *repogen.Team) any { return &i.DefaultEntity },

	"owner_uuid": func(i *repogen.Team) any { return &i.OwnerUUID },

	"status": func(i *repogen.Team) any { return &i.Status },

	"created_at": func(i *repogen.Team) any { return &i.CreatedAt },

	"updated_at": func(i *repogen.Team) any { return &i.UpdatedAt },

	"created_by_uuid": func(i *repogen.Team) any { return &i.CreatedByUUID },

	"updated_by_uuid": func(i *repogen.Team) any { return &i.UpdatedByUUID },
}
