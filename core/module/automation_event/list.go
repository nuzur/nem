// Code generated by nuzur go-code-gen. DO NOT EDIT.

package automation_event

import (
	"context"
	"database/sql"
	"fmt"
	main_entity "github.com/nuzur/nem/core/entity/automation_event"
	"github.com/nuzur/nem/core/module/automation_event/types"
	repogen "github.com/nuzur/nem/core/repository/gen"

	"go.uber.org/zap"
	"slices"
)

func (m *module) List(ctx context.Context,
	request types.ListRequest,
	opts ...Option) (types.ListResponse, error) {

	reqPlusOne := request
	reqPlusOne.PageSize = request.PageSize + 1

	query, err := m.repository.BuildListEntityQuery(
		ctx,
		reqPlusOne,
		main_entity.AutomationEvent{},
		false)
	if err != nil {

		return types.ListResponse{}, err
	}

	resolvedOpts := applyAllOptions(opts)
	cacheKey := fmt.Sprintf("ListAutomationEvent:%v", request)
	if !resolvedOpts.SkipCache {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.ListResponse), nil
		}
	}

	v, listErr, _ := m.sg.Do(cacheKey, func() (any, error) {
		var rows *sql.Rows
		rows, err = m.repository.DB.QueryContext(ctx, query)
		if err != nil {

			m.logger.Error("error in executing query for ListAutomationEvent", zap.String("query", query), zap.Error(err))
			return types.ListResponse{}, err
		}
		defer rows.Close()
		var scanGetters []func(*repogen.AutomationEvent) any
		if len(request.GetIncludeFields()) > 0 {
			for _, f := range listFields {
				if slices.Contains(request.GetIncludeFields(), f) {
					scanGetters = append(scanGetters, listFieldRegistry[f])
				}
			}
		} else if len(request.GetExcludeFields()) > 0 {
			for _, f := range listFields {
				if !slices.Contains(request.GetExcludeFields(), f) {
					scanGetters = append(scanGetters, listFieldRegistry[f])
				}
			}
		} else {
			for _, f := range listFields {
				scanGetters = append(scanGetters, listFieldRegistry[f])
			}
		}

		var items []repogen.AutomationEvent
		for rows.Next() {
			var i repogen.AutomationEvent
			fields := make([]any, 0, len(scanGetters))
			for _, getter := range scanGetters {
				fields = append(fields, getter(&i))
			}
			if err := rows.Scan(fields...); err != nil {

				return types.ListResponse{}, err
			}
			items = append(items, i)
		}
		if err := rows.Close(); err != nil {

			return types.ListResponse{}, err
		}
		if err := rows.Err(); err != nil {

			return types.ListResponse{}, err
		}

		hasNextPage := false
		if len(items) > int(request.PageSize) {
			hasNextPage = true
			items = items[:request.PageSize]
		}

		return types.ListResponse{
			AutomationEvent: mapModelsToEntities(items),
			HasNextPage:     hasNextPage,
		}, nil
	})
	if listErr != nil {
		return types.ListResponse{}, listErr
	}
	listResponse := v.(types.ListResponse)
	if !resolvedOpts.SkipCache {
		m.cache.Set(cacheKey, listResponse, 0)
	}
	return listResponse, nil
}

var listFields = []string{

	"uuid",

	"automation_uuid",

	"project_uuid",

	"change_request_uuid",

	"payload",

	"status",

	"attempts",

	"next_attempt_at",

	"delivered_at",

	"last_error",

	"created_at",

	"updated_at",

	"created_by_uuid",

	"updated_by_uuid",
}

var listFieldRegistry = map[string]func(*repogen.AutomationEvent) any{

	"uuid": func(i *repogen.AutomationEvent) any { return &i.UUID },

	"automation_uuid": func(i *repogen.AutomationEvent) any { return &i.AutomationUUID },

	"project_uuid": func(i *repogen.AutomationEvent) any { return &i.ProjectUUID },

	"change_request_uuid": func(i *repogen.AutomationEvent) any { return &i.ChangeRequestUUID },

	"payload": func(i *repogen.AutomationEvent) any { return &i.Payload },

	"status": func(i *repogen.AutomationEvent) any { return &i.Status },

	"attempts": func(i *repogen.AutomationEvent) any { return &i.Attempts },

	"next_attempt_at": func(i *repogen.AutomationEvent) any { return &i.NextAttemptAt },

	"delivered_at": func(i *repogen.AutomationEvent) any { return &i.DeliveredAt },

	"last_error": func(i *repogen.AutomationEvent) any { return &i.LastError },

	"created_at": func(i *repogen.AutomationEvent) any { return &i.CreatedAt },

	"updated_at": func(i *repogen.AutomationEvent) any { return &i.UpdatedAt },

	"created_by_uuid": func(i *repogen.AutomationEvent) any { return &i.CreatedByUUID },

	"updated_by_uuid": func(i *repogen.AutomationEvent) any { return &i.UpdatedByUUID },
}
