// Code generated by nuzur go-code-gen. DO NOT EDIT.

package enum

import (
	"github.com/nuzur/nem/core/entity/validation"
)

// Validate checks this Enum against the schema's field type and
// type-config rules, aggregating every problem into a single *validation.Error.
// It mirrors the change-request data validation enforced in
// product/module/datavalidation, so data written through this API is held to
// the same rules. Generated by go-code-gen — do not edit.
func (e Enum) Validate() error {
	c := validation.NewCollector()
	if e.UUID.IsNil() {
		c.Require("enum.uuid")
	}
	c.Field("enum.version", validation.Integer(e.Version, false, 0, 0, false, false, false, false, 0, 0))
	if e.Identifier == "" {
		c.Require("enum.identifier")
	} else {
		c.Field("enum.identifier", validation.String(e.Identifier, 0, 255, ""))

	}
	if len(e.StaticValues) == 0 {
		c.Require("enum.static_values")
	} else {
		for i, item := range e.StaticValues {
			c.Merge(validation.Index("enum.static_values", i), item.Validate())
		}

	}
	if e.Status.ToInt64() == 0 {
		c.Require("enum.status")
	} else {
		c.Field("enum.status", validation.EnumMember(e.Status.ToInt64(), []int64{0, 1, 2}, "enum_status"))

	}
	if e.CreatedAt.IsZero() {
		c.Require("enum.created_at")
	}
	if e.UpdatedAt.IsZero() {
		c.Require("enum.updated_at")
	}
	if e.CreatedByUUID.IsNil() {
		c.Require("enum.created_by_uuid")
	}
	if e.UpdatedByUUID.IsNil() {
		c.Require("enum.updated_by_uuid")
	}

	return c.Result()
}
