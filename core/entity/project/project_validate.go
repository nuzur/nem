// Code generated by nuzur go-code-gen. DO NOT EDIT.

package project

import (
	"github.com/nuzur/nem/core/entity/validation"
)

// Validate checks this Project against the schema's field type and
// type-config rules, aggregating every problem into a single *validation.Error.
// It mirrors the change-request data validation enforced in
// product/module/datavalidation, so data written through this API is held to
// the same rules. Generated by go-code-gen — do not edit.
func (e Project) Validate() error {
	c := validation.NewCollector()
	if e.UUID.IsNil() {
		c.Require("project.uuid")
	}
	if e.Name == "" {
		c.Require("project.name")
	} else {
		c.Field("project.name", validation.String(e.Name, 0, 255, ""))

	}
	if e.Description.Valid && e.Description.String != "" {
		c.Field("project.description", validation.String(e.Description.String, 0, 255, ""))

	}
	if len(e.Tags) > 0 {
		c.Field("project.tags", validation.Unique(e.Tags))
		for i, el := range e.Tags {
			c.Field(validation.Index("project.tags", i), validation.String(el, 0, 0, ""))
		}

	}
	if e.URL.Valid && e.URL.String != "" {
		c.Field("project.url", validation.String(e.URL.String, 0, 255, ""))

	}
	if e.OwnerUUID.IsNil() {
		c.Require("project.owner_uuid")
	}
	if e.TeamUUID.IsNil() {
		c.Require("project.team_uuid")
	}
	if e.AccessType.ToInt64() == 0 {
		c.Require("project.access_type")
	} else {
		c.Field("project.access_type", validation.EnumMember(e.AccessType.ToInt64(), []int64{0, 1, 2}, "project_access_type"))

	}
	for i, item := range e.ProjectExtensions {
		c.Merge(validation.Index("project.project_extensions", i), item.Validate())
	}
	if e.Status.ToInt64() == 0 {
		c.Require("project.status")
	} else {
		c.Field("project.status", validation.EnumMember(e.Status.ToInt64(), []int64{0, 1, 2}, "project_status"))

	}
	if e.CreatedByUUID.IsNil() {
		c.Require("project.created_by_uuid")
	}
	if e.UpdatedByUUID.IsNil() {
		c.Require("project.updated_by_uuid")
	}

	return c.Result()
}
